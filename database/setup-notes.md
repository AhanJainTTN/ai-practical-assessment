# Database Setup Notes

TBD — database choice and local setup instructions will be added after the RDBMS is locked.

Required deliverables:
- Schema or migration scripts (`database/schema-or-migrations/`)
- Seed data (`database/seed-data/`)
- Environment variable example (if applicable)
- Steps to run locally (documented in README)
